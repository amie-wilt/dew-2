<h1>Products#index</h1>
<p>Find me in app/views/products/index.html.erb</p>


<% @products.each do |p| %>
    <div class="product-container">
      <%= p.name %>
      <%= p.description %>
      <%= p.price %>
      <%= p.permalink %>
      <%= p.image %>
    </div>
<% end %>